{pkgs, lib, config, ...}:
let
    opt_name="NAME";
in
{
    options = {
        mods."${opt_name}".enable =
            lib.mkEnableOption "enables";
    };

    config = lib.mkIf config.mods."${opt_name}".enable {
        environment.systemPackages = [

        ];
    };
}
