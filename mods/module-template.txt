{pkgs, lib, config, ...}:
let
    opt_name="NAME";
in
{
    options = {
        "${opt_name}".enable =
            lib.mkEnableOption "enables";
    };

    config = lib.mkIf config."${opt_name}".enable {
        environment.systemPackages = [

        ];
    };
}
